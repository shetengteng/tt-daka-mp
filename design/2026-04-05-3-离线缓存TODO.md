# 离线缓存实施 TODO

> 基于 `2026-04-05-2-离线缓存设计.md`

## Phase 1：LocalStore + Store 拆分 + 读缓存（0.5 天）✅

- [x] 创建 `utils/local-store.js`（纯 localStorage 工具，不含业务快捷方法）
  - [x] `getStoreKey` / `setLocal` / `getLocal` / `removeLocal` / `clearUserCache`
- [x] 拆分 Store（替代 `stores/daka.js`）
  - [x] 创建 `stores/project.js` — list + activeList + restore/persist/setList
  - [x] 创建 `stores/record.js` — todayRecords + pendingCount + isOffline + todayProgress
  - [x] 各 Store 内部调用 `local-store.js` 实现 `restore()` / `persist()`
  - [x] 删除 `stores/daka.js`，迁移所有引用到新 Store
- [x] 改造 API 层（不直接调 local-store，只与 Store 交互）
  - [x] `getProjectList.js` → 成功后 `projectStore.setList()` + `recordStore.setTodayRecords()`
  - [x] `getProjectList.js` → 失败时返回 Store 已有缓存
  - [x] `getRecordsByMonth.js` → 成功后 `setLocal` 月度缓存 / 失败时 `getLocal` 降级
- [x] 改造 `App.vue` — `onLaunch` 中 `projectStore.restore()` + `recordStore.restore()`
- [x] 页面层迁移引用
  - [x] `pages/index/index.vue` → `useProjectStore` + `useRecordStore`
  - [x] `pages/calendar/index.vue` → `useProjectStore`
  - [x] `pages/stats/index.vue` → `useProjectStore`
  - [x] `pages/mine/index.vue` → `useProjectStore`

## Phase 2：PendingOps + toggleDaka Local-First + addBatch（1 天）✅

- [x] 创建 `utils/pending-ops.js`
  - [x] `getPendingOps` / `addPendingOp` / `removePendingOp` / `savePendingOps` / `clearPendingOps` / `getPendingCount`
- [x] 改造 `pages/index/api/toggleDaka.js`（Local-First）
  - [x] 打卡：`recordStore.addTodayRecord(record)` + `addPendingOp(add)` → `debouncedSync()`
  - [x] 取消：`recordStore.removeTodayRecord(projectId)` + `addPendingOp(remove)` → `debouncedSync()`
  - [x] 更新 `recordStore.pendingCount`
  - [x] 首页移除了外部 `addTodayRecord` / `removeTodayRecord` 调用（toggleDaka 内部已处理）
- [x] `database.js` 新增 `addBatch(dataList)` — 调用 EMAS `insertMany`

## Phase 3：SyncManager（1 天）✅

- [x] 创建 `utils/sync-manager.js`
  - [x] `syncPendingOps(accountId)` — 合并抵消 + 批量插入 + 重试 + Quota 检测
  - [x] `mergeOps(ops)` — 合并抵消算法（projectId + date 从 data 或 where 提取）
  - [x] `debouncedSync(accountId)` — 300ms 防抖（export 给 toggleDaka）
  - [x] 批量失败时 retryCount 持久化（`savePendingOps`）
  - [x] 同步成功后 `touchCloudVersion(accountId)` 更新云端版本号
- [x] 集成同步触发点
  - [x] `App.vue` onShow — pendingCount > 0 时自动同步
  - [x] 首页 `onPullDownRefresh` — 先 `syncPendingOps` 再 `loadData`
  - [x] `toggleDaka` 写操作后 — `debouncedSync()`

## Phase 4：版本号机制（0.5 天）✅

- [x] `dk-users` 表新增 `dataVersion` 字段 → `schema.js` DakaUser typedef 更新
- [x] `database.js` 新增 `field(projection)` 方法
- [x] 创建 `utils/version-check.js`
  - [x] `checkDataVersion(accountId)` — `.field({ dataVersion: 1 })` 只拉版本号
  - [x] `updateLocalVersion(accountId, version)` — 更新 `dk_{accountId}_meta_dataVersion`
  - [x] `touchCloudVersion(accountId)` — `update({ dataVersion: Date.now() })`，同时更新本地版本
- [x] 首页 onShow — 有缓存时检查版本号，`cloudVersion > localVersion` 才拉取

## Phase 5：UI 提示 + 边界处理（0.5 天）✅

- [x] 首页离线状态条
  - [x] `recordStore.pendingCount > 0` 时显示"N 条记录待同步"
  - [x] 提供"立即同步"按钮
- [x] 登出时处理未同步数据
  - [x] `pendingCount > 0` → `uni.showModal` 提醒"有 N 条记录未同步，退出后将丢失"
  - [x] 确认登出 → `clearPendingOps` + `clearUserCache` + 原有登出逻辑

## 已完成

- [x] 刷新缓存功能（我的页面"清除缓存"→"刷新缓存"）
- [x] EMAS Quota 错误检测（`PrePayResourceExhausted` → toast 提示）
- [x] onShow 智能判断（TTL + dirty + 版本号三重机制）
- [x] ensureUser 启动跳过（App.vue 不再调用 ensureUser）

## 注意事项

- **月度缓存**：`getRecordsByMonth` 的缓存不放 Store，直接用 `setLocal` / `getLocal`
- **`_id` 前缀**：Local-First 创建的记录 `_id` 以 `local_` 开头，同步时 `addBatch` 会移除
- **`debouncedSync` 跨模块**：`toggleDaka`（Phase 2）调用 `sync-manager`（Phase 3）的 `debouncedSync`
