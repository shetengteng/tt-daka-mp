# DaKa 功能测试用例

> 基于当前已实现功能的手工测试清单 + 单元测试覆盖情况

---

## 单元测试覆盖汇总

| 测试文件 | 测试对象 | 用例数 | 状态 |
|---|---|---|---|
| `tests/local-store.test.js` | `utils/local-store.js` — getStoreKey / setLocal / getLocal / removeLocal | 7 | ✅ 全部通过 |
| `tests/pending-ops.test.js` | `utils/pending-ops.js` — add / remove / save / clear / count | 7 | ✅ 全部通过 |
| `tests/auth.test.js` | `utils/auth.js` — accountId / loginType / isLoggedIn / clear | 8 | ✅ 全部通过 |
| `tests/error.test.js` | `cloud-emas/database/error.js` — isQuotaError / handleEmasError | 10 | ✅ 全部通过 |
| `tests/error-extended.test.js` | `cloud-emas/database/error.js` — checkEmasError | 5 | ✅ 全部通过 |
| `tests/sync-manager.test.js` | `utils/sync-manager.js` — mergeOps 抵消算法 | 6 | ✅ 全部通过 |
| `tests/stores.test.js` | `stores/user.js` + `stores/stats.js` — Pinia Store 操作 | 9 | ✅ 全部通过 |
| `tests/project-store.test.js` | `stores/project.js` — setList / activeList / cache / persist / restore | 11 | ✅ 全部通过 |
| `tests/record-store.test.js` | `stores/record.js` — todayRecords / progress / add / remove / cache / persist | 13 | ✅ 全部通过 |
| `tests/theme-store.test.js` | `stores/theme.js` — mode / toggle / setMode / colors / themeStyle | 7 | ✅ 全部通过 |
| `tests/date.test.js` | `utils/date.js` — formatDate / formatDateTime / formatMonth / getMonthLabel / isToday / formatRelativeDate / getDateRange | 17 | ✅ 全部通过 |
| `tests/file.test.js` | `utils/file.js` — isLocalFile | 7 | ✅ 全部通过 |
| `tests/version-check.test.js` | `utils/version-check.js` — updateLocalVersion | 3 | ✅ 全部通过 |
| **合计** | | **110** | **✅ 全部通过** |

---

## 1. 登录模块

### 1.1 微信登录
- [ ] 首次打开 → 跳转登录页
- [ ] 勾选隐私协议后点击"微信一键登录" → 登录成功 → 跳转首页
- [ ] 未勾选协议点击登录 → 提示需先同意
- [ ] 登录后退出再重新登录 → 账号数据不丢失（openid 匹配）

### 1.2 匿名登录（H5 开发模式）
- [ ] 点击"开发模式登录" → 使用测试账号登录
- [ ] 登录后各页面功能正常

### 1.3 登录状态恢复 `🧪 auth.test.js`
- [x] accountId 持久化存储与恢复（单元测试覆盖）
- [x] loginType 默认 anonymous / 设置 / 清除（单元测试覆盖）
- [x] isLoggedIn 状态判断（单元测试覆盖）
- [x] clearAccountId 清理完整性（单元测试覆盖）
- [ ] 已登录状态下杀掉 App 重新打开 → 自动恢复授权（手工验证）
- [ ] 授权恢复失败时 → 不影响本地缓存展示（手工验证）

---

## 2. 首页（打卡列表）

### 2.1 项目列表展示 `🧪 project-store.test.js`
- [x] activeList 过滤归档项目并按 sortOrder 排序（单元测试覆盖）
- [ ] 无打卡项目 → 显示空状态 + "创建打卡项目"按钮（手工验证）
- [ ] 有项目 → 按 sortOrder 排序展示（手工验证）

### 2.2 今日进度 `🧪 record-store.test.js`
- [x] todayProgress 根据 activeList 和 todayRecords 计算正确百分比（单元测试覆盖）
- [x] 无活跃项目时 progress 为 0/0/0（单元测试覆盖）
- [x] 全部完成时 progress 为 100%（单元测试覆盖）
- [ ] 进度条 UI 展示（手工验证）

### 2.3 打卡操作（Local-First）`🧪 record-store.test.js / pending-ops.test.js`
- [x] addTodayRecord 追加记录（单元测试覆盖）
- [x] removeTodayRecord 按 projectId 删除（单元测试覆盖）
- [x] addPendingOp 写入 pending 队列并自动生成 id（单元测试覆盖）
- [ ] 点击未打卡的卡片 → 震动反馈 + 立即标记（手工验证）
- [ ] 打卡后 UI 立即响应（手工验证）
- [ ] 点击已打卡卡片 → 弹出取消确认弹窗（手工验证）

### 2.4 离线状态条
- [ ] 有 pending 记录时 → 显示"N 条记录待同步"
- [ ] 点击"立即同步" → 触发同步并更新计数
- [ ] pending 清空后 → 离线状态条消失

### 2.5 下拉刷新
- [ ] 下拉刷新 → 先同步 pending → 再从云端拉取最新数据
- [ ] 刷新完成后停止下拉动画

### 2.6 版本号检查 `🧪 version-check.test.js`
- [x] updateLocalVersion 正确存储版本号（单元测试覆盖）
- [x] 覆写旧版本号（单元测试覆盖）
- [ ] Tab 切换回首页时 → 检查 dataVersion（手工验证）
- [ ] 云端版本号 > 本地 → 自动拉取最新数据（手工验证）
- [ ] 云端版本号 ≤ 本地 → 跳过拉取（手工验证）

### 2.7 卡片长按操作
- [ ] 长按卡片 → 弹出操作菜单（编辑/归档/删除）
- [ ] 选择"编辑" → 跳转编辑页
- [ ] 选择"归档" → 确认后归档成功 → 列表刷新
- [ ] 选择"删除" → 确认后删除成功 → 列表刷新

---

## 3. 日历页

### 3.1 月度视图
- [ ] 默认显示当月日历
- [ ] 全部完成的日期 → 绿色标记 + 显示"完成数/总数"
- [ ] 部分完成的日期 → 橙色标记
- [ ] 未打卡的日期 → 无标记

### 3.2 日期选择
- [ ] 点击某一天 → 下方显示该日详情
- [ ] 详情列出所有活跃项目的打卡状态

### 3.3 月份切换
- [ ] 切换到上/下月 → 重新加载该月数据
- [ ] 月度缓存生效 → 切回已加载过的月份时从本地缓存读取

### 3.4 本月统计
- [ ] 底部显示"本月 X/Y 天 · 完成率 Z%"
- [ ] 仅统计到今天为止的天数

---

## 4. 统计页

### 4.1 总览卡片 `🧪 stores.test.js`
- [x] statsStore.setStats 正确更新 totalDays / currentStreak / longestStreak（单元测试覆盖）
- [x] statsStore.isLoaded 反映加载状态（单元测试覆盖）
- [x] statsStore.clear 重置所有字段（单元测试覆盖）
- [ ] UI 显示"总打卡天数"、"最长连续"、"当前连续"（手工验证）

### 4.2 本周完成率柱状图
- [ ] 显示最近 7 天的完成率柱状
- [ ] 100% 完成 → 绿色柱
- [ ] 部分完成 → 橙色柱
- [ ] 未来日期 → 灰色 + 显示"--"

### 4.3 各项目统计卡片
- [ ] 每个项目显示：完成率、连续天数、最长连续、本月打卡数
- [ ] 最近 7 天圆点：打卡 → 实心（项目颜色），未打卡 → 空心
- [ ] 无项目时显示"暂无统计数据"

### 4.4 下拉刷新
- [ ] 下拉刷新 → 重新加载统计数据

---

## 5. 我的页面

### 5.1 用户信息卡片 `🧪 stores.test.js`
- [x] userStore 默认值正确：nickname="微信用户", avatar="", totalDays=0（单元测试覆盖）
- [x] userStore.setUser 更新字段（单元测试覆盖）
- [x] userStore.setUser 部分更新保留其他字段（单元测试覆盖）
- [x] userStore.isLoaded 状态判断（单元测试覆盖）
- [x] userStore.clear 重置所有字段（单元测试覆盖）
- [ ] UI 显示头像、昵称、"已坚持打卡 X 天"（手工验证）
- [ ] 点击卡片 → 弹出编辑弹窗（手工验证）

### 5.2 编辑个人信息 `🧪 file.test.js`
- [x] isLocalFile 识别 wxfile:// / blob: / /tmp / http://tmp 路径（单元测试覆盖）
- [x] isLocalFile 排除 https / base64 Data URI（单元测试覆盖）
- [ ] 弹窗显示当前头像和昵称（手工验证）
- [ ] 更换头像 → 选择图片 → 压缩 → base64 转换（手工验证）
- [ ] 修改昵称 → 保存成功 → Store 更新 → UI 更新（手工验证）
- [ ] 昵称为空时保存 → 提示"昵称不能为空"（手工验证）

### 5.3 打卡管理 `🧪 stores.test.js`
- [x] statsStore.setMineCounts 更新 activeCount / archivedCount（单元测试覆盖）
- [ ] 显示"打卡项目管理"（带活跃项目数 badge）（手工验证）
- [ ] 显示"已归档项目"（带归档数 badge）（手工验证）
- [ ] 点击跳转到对应管理页面（手工验证）

### 5.4 深色模式 `🧪 theme-store.test.js`
- [x] 默认 light 模式（单元测试覆盖）
- [x] toggle 切换 light ↔ dark（单元测试覆盖）
- [x] setMode 设置指定模式（单元测试覆盖）
- [x] dark 模式下颜色变量不同于 light（单元测试覆盖）
- [x] themeStyle 在 dark 模式生成 CSS 变量字符串（单元测试覆盖）
- [ ] 所有页面和组件颜色正确响应（手工验证）
- [ ] 弹窗（ProfileEditor 等 fixed 元素）主题正确（手工验证）

### 5.5 刷新缓存
- [ ] 点击"刷新缓存" → 确认弹窗
- [ ] 确认后 → markDirty + 重新加载数据 → 提示"缓存已刷新"

### 5.6 退出登录
- [ ] 无 pending 数据时 → 确认后直接退出
- [ ] 有 pending 数据时 → 弹出"有 N 条记录未同步，退出后将丢失"提醒
- [ ] 确认退出 → 清理 pending + 缓存 + 登录状态 → 跳转登录页

---

## 6. 项目管理

### 6.1 项目列表
- [ ] 显示所有活跃项目 + 打卡天数
- [ ] 提供编辑、归档、删除按钮

### 6.2 拖拽排序
- [ ] 长按项目卡片 → 进入拖拽模式
- [ ] 拖动到目标位置 → 显示绿色插入线
- [ ] 松手后 → 更新排序 → 调用 batchUpdateSort API
- [ ] 只有 1 个项目时不可拖拽

### 6.3 创建/编辑项目
- [ ] 创建页面：填写名称、选择图标、选择颜色、选择频率
- [ ] 编辑页面：回显已有数据 → 修改后保存

### 6.4 归档/取消归档
- [ ] 归档项目 → 从首页列表移除
- [ ] 已归档页面 → 可恢复归档项目

### 6.5 删除项目
- [ ] 确认删除 → 项目和所有打卡记录一并删除

---

## 7. 离线缓存 & 同步

### 7.1 Store 持久化 `🧪 local-store.test.js / project-store.test.js / record-store.test.js`
- [x] setLocal / getLocal 正确读写带 metadata 的数据（单元测试覆盖）
- [x] getStoreKey 生成正确的 key 格式（单元测试覆盖）
- [x] removeLocal 正确删除数据（单元测试覆盖）
- [x] 不存在的 key 返回 null（单元测试覆盖）
- [x] projectStore persist / restore 往返正确（单元测试覆盖）
- [x] recordStore persist / restore 往返正确（单元测试覆盖）
- [x] 无 accountId 时 restore 不报错（单元测试覆盖）
- [ ] 杀掉 App 重新打开 → 页面立即显示缓存数据（手工验证）

### 7.2 缓存有效性（TTL + dirty）`🧪 project-store.test.js / record-store.test.js`
- [x] isCacheValid 初始为 false（单元测试覆盖）
- [x] markFresh 后（有数据）isCacheValid 为 true（单元测试覆盖）
- [x] markDirty 使缓存失效（单元测试覆盖）
- [x] 空列表时即使 markFresh 也不算有效（单元测试覆盖）
- [ ] TTL 过期 → 自动重新拉取（手工验证）

### 7.3 PendingOps 队列 `🧪 pending-ops.test.js`
- [x] addPendingOp 自动生成 id / timestamp / retryCount（单元测试覆盖）
- [x] 多次 addPendingOp 累计（单元测试覆盖）
- [x] removePendingOp 按 id 精确删除（单元测试覆盖）
- [x] savePendingOps 批量覆写（单元测试覆盖）
- [x] clearPendingOps 清空（单元测试覆盖）
- [x] getPendingCount 空账号返回 0（单元测试覆盖）
- [ ] 打卡写入 pending 队列 → localStorage 持久化（手工验证）

### 7.4 SyncManager 同步 `🧪 sync-manager.test.js`
- [x] mergeOps: add + remove 同 projectId + date 自动抵消（单元测试覆盖）
- [x] mergeOps: 不同 date 不会抵消（单元测试覆盖）
- [x] mergeOps: 多个 add + 1 个 remove 只抵消一对（单元测试覆盖）
- [x] mergeOps: 非 dk-records 集合不参与抵消（单元测试覆盖）
- [x] mergeOps: 空数组返回空数组（单元测试覆盖）
- [ ] debouncedSync 300ms 防抖效果（手工验证）
- [ ] add 操作批量插入（insertMany）（手工验证）
- [ ] 同步成功 → touchCloudVersion + toast 提示（手工验证）
- [ ] Quota 错误 → 立即停止 + 显示额度用完提示（手工验证）

### 7.5 版本号机制 `🧪 version-check.test.js`
- [x] updateLocalVersion 正确存储版本号（单元测试覆盖）
- [x] 覆写旧版本号（单元测试覆盖）
- [ ] 写操作同步后 → 云端 dataVersion 更新为 Date.now()（手工验证）
- [ ] 读操作前先查 dataVersion → 使用 projection 只取版本号（手工验证）
- [ ] cloudVersion > localVersion → 触发全量拉取（手工验证）

### 7.6 月度缓存
- [ ] 成功加载某月数据后 → setLocal 缓存
- [ ] API 失败时 → getLocal 降级显示缓存数据

### 7.7 App 生命周期触发
- [ ] App onShow → 检查 pending 队列 → 有数据则自动同步
- [ ] 首页 onPullDownRefresh → 先同步 pending → 再拉取数据

---

## 8. EMAS 错误处理 `🧪 error.test.js / error-extended.test.js`

### 8.1 Quota 超限
- [x] isQuotaError 检测 PrePayResourceExhausted code（单元测试覆盖）
- [x] isQuotaError 检测 HTTP 503（单元测试覆盖）
- [x] isQuotaError 检测 HTTP 429（单元测试覆盖）
- [x] isQuotaError 检测 quota / rate limit / exhausted 关键字（单元测试覆盖）
- [x] isQuotaError 正常错误返回 false（单元测试覆盖）
- [x] isQuotaError null/undefined 返回 false（单元测试覆盖）
- [x] handleEmasError quota 错误抛出友好消息（单元测试覆盖）
- [x] handleEmasError 非 quota 错误原样抛出（单元测试覆盖）
- [x] checkEmasError null 结果抛出空结果错误（单元测试覆盖）
- [x] checkEmasError 含 error 字段时抛出失败信息（单元测试覆盖）
- [x] checkEmasError 正常结果不抛出（单元测试覆盖）
- [ ] 显示"今日云服务请求额度已用完" toast（手工验证）
- [ ] 5 秒内不重复弹 toast（手工验证）

### 8.2 网络错误降级
- [ ] getProjectList 失败 → 返回 Store 中的缓存数据（fromCache: true）
- [ ] getRecordsByMonth 失败 → 返回 localStorage 中的月度缓存

---

## 9. 主题 & UI

### 9.1 深色模式适配
- [ ] 所有页面背景、文字颜色正确
- [ ] 卡片、列表项背景色正确
- [ ] 弹窗（Dialog / ProfileEditor / ActionSheet）颜色正确
- [ ] 进度条、柱状图颜色正确
- [ ] 图标颜色适配

### 9.2 胶囊按钮适配
- [ ] 微信小程序中首页 header 不遮挡右上角胶囊

### 9.3 全局工具类
- [ ] gap-xs/sm/md/lg 生效
- [ ] px-xs、mx-xl 生效
- [ ] 无页面级重复的 .block / .gap-sm 定义

---

## 10. 日期工具 `🧪 date.test.js`

- [x] formatDate 输出 YYYY-MM-DD（单元测试覆盖）
- [x] formatDateTime 输出 YYYY-MM-DD HH:mm（单元测试覆盖）
- [x] formatMonth 输出 YYYY-MM（单元测试覆盖）
- [x] getMonthLabel 输出中文年月（单元测试覆盖）
- [x] isToday 今天返回 true、昨天返回 false（单元测试覆盖）
- [x] formatRelativeDate 今天/昨天/明天/其他日期（单元测试覆盖）
- [x] getDateRange 月份范围正确（单元测试覆盖）
- [x] 所有函数 falsy 输入返回空字符串/空对象（单元测试覆盖）
